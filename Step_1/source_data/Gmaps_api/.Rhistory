# Set the working directory where the this script file is located.
setwd("source_data\Gmaps API\")
# Set the working directory where the this script file is located.
setwd("source_data/Gmaps_api/")
# List all files in the current directory
file_list <- list.files()
# Read each file in the list of files and append them into one data frame
# read_delim() is used with a semi-colon (;) delimiter to read the file
all_data <- lapply(file_list, function(file) {
read_delim(file, col_types = cols(.default = "c"), delim = ';')
}) %>% bind_rows() # bind_rows() combines all data frames in the list into a single data frame
# Import necessary libraries
library(tidyverse)
library(readr)
# Set the working directory where the this script file is located.
setwd("source_data/Gmaps_api/")
# List all files in the current directory
file_list <- list.files()
# Read each file in the list of files and append them into one data frame
# read_delim() is used with a semi-colon (;) delimiter to read the file
all_data <- lapply(file_list, function(file) {
read_delim(file, col_types = cols(.default = "c"), delim = ';')
}) %>% bind_rows() # bind_rows() combines all data frames in the list into a single data frame
# Remove duplicate rows in the data frame based on all columns
unique_data <- all_data %>%
distinct()
# Selecting the output folder:
setwd("outputs/")
# Selecting the output folder:
setwd("outputs/")
# Selecting the output folder:
setwd("output/")
dir.exists("output/")
# Set the working directory where the Gmaps file are located.
setwd("source_data/Gmaps_api/")
# Set the working directory where the Gmaps file are located.
setwd("source_data/Gmaps_api/")
source("C:/Users/radok/OneDrive/Desktop/Maastricht Univeristy/Service Project/Business Analytics/ZIP File for BA/Step_1/2 - Gmaps-Concatenate Files.R", echo=TRUE)
# Set the working directory where the Gmaps file are located.
setwd()
# Set the working directory where the Gmaps file are located.
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# List all files in the current directory
file_list <- list.files()
# Set the working directory where the Gmaps file are located.
setwd("source_data/Gmaps_api/")
# List all files in the current directory
file_list <- list.files()
# Read each file in the list of files and append them into one data frame
# read_delim() is used with a semi-colon (;) delimiter to read the file
all_data <- lapply(file_list, function(file) {
read_delim(file, col_types = cols(.default = "c"), delim = ';')
}) %>% bind_rows() # bind_rows() combines all data frames in the list into a single data frame
# Remove duplicate rows in the data frame based on all columns
unique_data <- all_data %>%
distinct()
# Selecting the output folder:
setwd("output/")
